const contador = () => {
    let contador = 0;
    // const aumentar = () => contador++
    return (
        <>
            <h2>contador</h2>   
            <h3>{contador}</h3>
        </>
    );
};

export default contador
